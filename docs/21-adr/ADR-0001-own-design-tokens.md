# ADR-0001: Собственная система токенов вместо правки значений Multica

Дата: 25 сентября 2026 года
Статус: Accepted

## Контекст

`packages/ui/styles/tokens.css` унаследован от Multica: нейтрали на hue 60, оранжевый brand `oklch(0.705 0.187 47.6)`, радиус 0.625rem, набор имён токенов shadcn. Замена отдельных значений уже делалась (шрифты, иконка) и не изменила восприятие: экраны накладываются на Multica один в один.

## Рассмотренные варианты

- Правка значений в существующих токенах — быстро, ноль регрессий / силуэт и семантика цвета те же, статусы по-прежнему цветные бейджи;
- Новая система с другой семантикой (полоса статуса, одна тёплая точка акцента, тёмная рельса) поверх тех же имён токенов — отличие видно, имена токенов стабильны для компонентов / часть компонентов потребует правки стилей, 3–4 дня;
- Полностью новый набор имён токенов — максимальная свобода / правка каждого компонента `packages/ui`, недели.

## Решение

Новая система «Речная синь» с другой семантикой цвета при сохранении имён токенов; новые токены (`--accent-beak`, `--rail`, `--status-*`) добавляются к существующим.

## Последствия

Положительные: компоненты shadcn продолжают работать без переименований; смена восприятия достигается семантикой, а не только значениями.
Отрицательные: часть компонентов имеет захардкоженные `bg-orange-*` и подобные классы, их придётся найти и заменить (T-002).
Что станет сигналом пересмотреть решение: если после T-001..T-003 скриншоты списка задач Goosar и Multica при наложении совпадают по положению более чем половины элементов, семантики недостаточно и нужна новая композиция раньше, чем планировалось.
