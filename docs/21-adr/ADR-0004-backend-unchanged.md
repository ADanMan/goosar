# ADR-0004: Бэкенд, API и схема БД не меняются; чистая комната отложена

Дата: 25 сентября 2026 года
Статус: Accepted

## Контекст

Замер 25 сентября 2026: `server/internal/handler` совпадает с Multica на 81%, `daemon` на 72%, `service` на 73%. Допсоглашение V2 с Multica разрешает производную работу, переименование и MIT-редистрибуцию. Владелец хочет позиционировать Goosar как отдельный продукт; полная реимплементация оценена в месяцы.

## Рассмотренные варианты

- Переработать только поверхность (ui, views, web, desktop) — 12–16 дней, ноль регрессий на сервере, откат без миграций / аудит кода продолжит показывать родство в `server/`;
- Поверхность плюс переименование API-путей и полей JSON — внешне другой API / ломает CLI, desktop, документацию и существующие установки, не меняет структуру кода, аудит всё равно найдёт совпадения;
- Чистая комната: спецификация поведения и реализация независимым исполнителем — единственный путь к «независимому продукту» / месяцы, заморозка фич, два человека.

## Решение

В версии 1.1 меняется только поверхность; `server/`, `packages/core`, HTTP API и схема остаются как есть.

## Последствия

Положительные: релиз 1.1.0 без миграций и без изменений для существующих установок; тесты `server/` не трогаются.
Отрицательные: в публичных заявлениях нельзя утверждать «независимая реализация»; корректная формулировка: «продукт на основе открытого кода Multica по лицензионному соглашению, с собственным интерфейсом и российской on-prem поставкой».
Что станет сигналом пересмотреть решение: письменное требование Multica прекратить использование кода, либо коммерческий контракт, где заказчик требует отсутствия чужого кода в `server/`. В обоих случаях запускается чистая комната по отдельному паку в режиме new.
