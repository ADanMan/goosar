# Goosar как самостоятельный продукт: сводка пака

Актуальность: 25 сентября 2026 года. Режим сборки: update. Версия: 1.1.0 (целевая).

## 1. Что это

Goosar — платформа управления задачами, где агенты стоят в списке исполнителей наравне с людьми. Кодовая база унаследована от Multica (Index Labs) по допсоглашению V2 и сейчас совпадает с ней по коду на 71% и по экранам почти полностью. Пак описывает переработку визуала и продуктовой поверхности так, чтобы Goosar читался как другой продукт, при сохранении бэкенда и схемы БД.

## 2. Текущий статус

- Релиз 1.0.1 опубликован, прод goosar.ru работает, дистрибутив собран для пяти платформ.
- Бренд (имя, иконка, домен) свой; палитра, шрифты заголовков, раскладка экранов, навигация и термины — Multica с косметикой.
- Замер сходства: 1662 из 2059 файлов по тем же путям, `packages/views` 68%, `packages/ui` 61%, лендинг 59%.
- Работа по паку не начата. Ветка `redesign/1.1` не создана. Пуш запрещён до отдельного решения.

## 3. Ключевые решения

- Своя дизайн-система на токенах, а не правка значений Multica: ADR-0001.
- Навигация «рельса + контекстная панель» вместо левого сайдбара со списком разделов: ADR-0002.
- Русский словарь терминов первичен, английский переводится с него; `issue` уходит из UI: ADR-0003.
- Бэкенд, HTTP API и схема БД не меняются; отличие ограничено слоями `packages/ui`, `packages/views`, `apps/web`, `apps/desktop`: ADR-0004.
- Список по умолчанию вместо доски, карточка со статусной полосой вместо цветных бейджей: раздел 4 архитектуры.

## 4. Ближайший шаг

Создать ветку `redesign/1.1` от `v1.0.1` и выполнить тикет T-001 (новые токены в `packages/ui/styles/tokens.css`) с визуальной проверкой на `/{workspace}/issues`.

## 5. Состав пака

| Файл | О чём | Когда перечитывать |
|---|---|---|
| `20-architecture.md` | Что и как меняем, границы, референсы | Перед каждым эпиком |
| `21-adr/ADR-0001..0004` | Четыре решения с условиями пересмотра | При споре «а почему так» |
| `30-implementation-plan.md` | Эпики, порядок, критический путь | В начале недели |
| `31-backlog.md` | 18 тикетов с acceptance criteria | Перед каждой сессией |
| `32-test-plan.md` | Матрица и обязательные проверки | Перед релизом 1.1.0 |
| `41-changelog.md` | Unreleased-секция, пополняется по ходу | После каждого тикета |
| `43-release-checklist.md` | Контроль перед выкаткой 1.1.0 | В день релиза |

## 6. Открытые вопросы

- Лицензионный аудит (license_audit.py) по текущему дереву ещё не завершён; его вывод по совпадениям с multica ожидаемо «производная работа». Пак это не меняет, но результат нужно приложить к ADR-0004.
- Название сущности `squad` по-русски: в паке «Отряд». Требует подтверждения владельцем до T-006, иначе словарь переписывать дважды.
- Мобильное приложение (`apps/mobile`) в объём не входит; когда оно будет выпускаться, токены из T-001 переносить в `apps/mobile/global.css` (формат HSL, не oklch).
