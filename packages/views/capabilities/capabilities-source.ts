/**
 * Признак того, что читатель попал на страницу возможностей из баннера-
 * напоминания об учётных данных, а не другим путём.
 *
 * Баннер добавляет параметр в ссылку, страница его читает — больше он нигде
 * не используется, поэтому вынесен в отдельный модуль: баннеру не нужно
 * импортировать саму страницу, чтобы знать имя query-параметра.
 */
export const CAPABILITIES_SOURCE_PARAM = 'from';
export const CAPABILITIES_SOURCE_REMINDER = 'keys';
