# Grafana datasource provisioning for the Compose monitoring overlay (#396).
apiVersion: 1

datasources:
  - name: Prometheus
    uid: goosar-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
